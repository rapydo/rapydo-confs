
version: '3'

services:

  formatter:
    image: unibeautify/black:latest
    command: -S .
    volumes:
      # main config
      - ${SUBMODULE_DIR}/rapydo-confs/confs/ormatter.toml:/workdir/pyproject.toml
      # base dirs
      - ${PROJECT_DIR}/backend/apis:/workdir/api
      - ${PROJECT_DIR}/backend/tests:/workdir/tests
    working_dir: /workdir
